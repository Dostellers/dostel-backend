const dbConfig = require('./dbConfig');

module.exports = {
    dbConfig
    // You can add other configurations here as you expand your project.
    // e.g.
    // appConfig,
    // apiConfig,
    // authConfig,
};
